# `deps/shui`

This package provides reusable UI components.

## Role

- versioned UI components
- shared props/context convention
- common component primitives for the app

## Design choice

The package is versioned because the UI API is meant to be consumed from multiple places without forcing lockstep changes everywhere.

