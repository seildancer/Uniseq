# `deps/db`

This package defines the shared database schema and rules.

## Role

- DataScript schema
- rules
- default built-in pages
- shared DB API surface

## Design choice

The DB package is intentionally narrow so the frontend and CLI-compatible tools can share a consistent model.

