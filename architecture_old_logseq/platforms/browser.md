# Browser

The browser target uses the same app logic but relies on web-friendly storage and file abstractions.

## Constraints

- IndexedDB support
- browser routing
- limited filesystem access
- web-compatible rendering and event APIs

