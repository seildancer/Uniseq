# Sync Architecture

Sync is a paid feature candidate, but the app must remain local-first.

The server helps move encrypted or authenticated file changes between devices. It should not become the primary content model.

