# Plugin Architecture

The app does not need Logseq plugin compatibility.

It does need a future JS plugin surface strong enough to support workflows users expect from note apps.

Plugins run in TypeScript/JavaScript land and talk to the Rust engine through capability APIs.

