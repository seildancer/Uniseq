# Domain Model

The domain model is markdown-first and projection-driven.

The durable files are simple. The app builds richer indexes and views from them.

Core modeling concerns include:

- page identity by title and namespace
- page hierarchy and placement
- frontend-derived references and rollups
- markdown-owned page content and metadata
