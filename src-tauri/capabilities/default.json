{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for the main Uniseq window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
